server:
  port: ${PORT:4000}

spring:
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/fsad_feedback}

app:
  frontend-url: ${FRONTEND_URL:http://localhost:5173}
  auth:
    admin-login-email: ${ADMIN_LOGIN_EMAIL:soumya.mishra.7812@gmail.com}
    admin-login-password: ${ADMIN_LOGIN_PASSWORD:321123}
    secure-cookies: ${SECURE_COOKIES:false}
  jwt:
    access-secret: ${JWT_SECRET:development_secret}
    refresh-secret: ${REFRESH_JWT_SECRET:development_refresh_secret}
    access-expiration: ${JWT_EXPIRES_IN:15m}
    refresh-expiration: ${REFRESH_JWT_EXPIRES_IN:7d}

logging:
  level:
    org.springframework.security: INFO
